\svnInfo $Id$

%%
%%  Section: Linear Coordinate
%%

As already indicated by the name, this group encodes the properties of a simple
linear coordinate (or a number thereof, as multiple axes are
permitted). The attributes storing the actual coordinate parameters
are listed in Tab.~\ref{tab:coord-linear} below.

\input coordinates_coord_linear_table
